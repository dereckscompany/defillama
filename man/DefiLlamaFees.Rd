% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DefiLlamaFees.R
\name{DefiLlamaFees}
\alias{DefiLlamaFees}
\title{DefiLlamaFees: Protocol Fees and Revenue}
\description{
Retrieves DefiLlama's fees/revenue overview: the per-protocol fees breakdown (one
row per protocol, with trailing 24h/7d/30d/1y/all-time totals) and the aggregate
daily fees time series, optionally scoped to a single chain. All keyless.
}
\details{
The \code{data_type} argument (one of \link{FEES_DATA_TYPES}) selects what the totals mean —
\code{dailyFees} (total fees paid by users), \code{dailyRevenue} (the protocol's share), or
\code{dailyHoldersRevenue} (the share flowing to token holders) — and is passed verbatim
to the DefiLlama \code{dataType} query parameter. The per-protocol \verb{total*} columns are
measurements typed \verb{| NA}.

Inherits from \link{DefiLlamaBase}; all methods honour the \code{async} flag set at
construction (sync returns a \code{data.table}, async a promise).
}
\examples{
\dontrun{
fees <- DefiLlamaFees$new()
# Per-protocol fees breakdown across all chains:
fees$get_fees_overview()
# Protocol revenue (not total fees) on one chain:
fees$get_fees_overview(chain = "ethereum", data_type = "dailyRevenue")
# The aggregate daily fees history:
fees$get_fees_chart()
}

}
\section{Super classes}{
\code{\link[connectcore:RestClient]{connectcore::RestClient}} -> \code{\link[defillama:DefiLlamaBase]{defillama::DefiLlamaBase}} -> \code{DefiLlamaFees}
}
\section{Methods}{
\subsection{Public methods}{
\itemize{
\item \href{#method-DefiLlamaFees-new}{\code{DefiLlamaFees$new()}}
\item \href{#method-DefiLlamaFees-get_fees_overview}{\code{DefiLlamaFees$get_fees_overview()}}
\item \href{#method-DefiLlamaFees-get_fees_chart}{\code{DefiLlamaFees$get_fees_chart()}}
\item \href{#method-DefiLlamaFees-clone}{\code{DefiLlamaFees$clone()}}
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-DefiLlamaFees-new"></a>}}
\if{latex}{\out{\hypertarget{method-DefiLlamaFees-new}{}}}
\subsection{Method \code{new()}}{
Initialise a DefiLlamaFees client.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{DefiLlamaFees$new(
  base_url = defillama_base_url(),
  async = FALSE,
  max_tries = 3L
)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{base_url}}{(scalar<character>) the main API base URL. Defaults to
\code{\link[=defillama_base_url]{defillama_base_url()}}.}

\item{\code{async}}{(scalar<logical>) if \code{TRUE}, methods return promises. Default
\code{FALSE}.}

\item{\code{max_tries}}{(scalar<count in [1, Inf[>) retry budget for the idempotent
GET requests. Default \code{3}.}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
(class<DefiLlamaFees>) invisibly, self.
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-DefiLlamaFees-get_fees_overview"></a>}}
\if{latex}{\out{\hypertarget{method-DefiLlamaFees-get_fees_overview}{}}}
\subsection{Method \code{get_fees_overview()}}{
The per-protocol fees breakdown: one row per protocol with its
trailing 24h/7d/30d/1y/all-time totals (fees or revenue per \code{data_type}).
Optionally scoped to a single chain.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{DefiLlamaFees$get_fees_overview(
  chain = NULL,
  data_type = FEES_DATA_TYPES[["daily_fees"]]
)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{chain}}{(scalar<character> | NULL) restrict to one chain, e.g.
\code{"ethereum"}; \code{NULL} (default) covers all chains.}

\item{\code{data_type}}{(scalar<character>) the fees series to report, one of
\link{FEES_DATA_TYPES}. Default \code{"dailyFees"}.}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
(DimensionOverview | promise<DimensionOverview>) one row per protocol,
or a promise thereof.
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-DefiLlamaFees-get_fees_chart"></a>}}
\if{latex}{\out{\hypertarget{method-DefiLlamaFees-get_fees_chart}{}}}
\subsection{Method \code{get_fees_chart()}}{
The aggregate daily fees time series (the overview's
\code{totalDataChart}), one row per UTC day. Optionally scoped to a single chain.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{DefiLlamaFees$get_fees_chart(
  chain = NULL,
  data_type = FEES_DATA_TYPES[["daily_fees"]]
)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{chain}}{(scalar<character> | NULL) restrict to one chain; \code{NULL} (default)
covers all chains.}

\item{\code{data_type}}{(scalar<character>) the fees series to report, one of
\link{FEES_DATA_TYPES}. Default \code{"dailyFees"}.}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
(DimensionChart | promise<DimensionChart>) one row per UTC day, or a
promise thereof.
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-DefiLlamaFees-clone"></a>}}
\if{latex}{\out{\hypertarget{method-DefiLlamaFees-clone}{}}}
\subsection{Method \code{clone()}}{
The objects of this class are cloneable with this method.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{DefiLlamaFees$clone(deep = FALSE)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{deep}}{Whether to make a deep clone.}
}
\if{html}{\out{</div>}}
}
}
}
