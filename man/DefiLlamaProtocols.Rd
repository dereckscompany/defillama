% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DefiLlamaProtocols.R
\name{DefiLlamaProtocols}
\alias{DefiLlamaProtocols}
\title{DefiLlamaProtocols: DeFi Protocol and Chain Total Value Locked (TVL)}
\description{
Retrieves DefiLlama's TVL data: the list of every tracked DeFi protocol with its
current TVL and identity, one protocol's full historical TVL, the current
aggregate TVL of every chain, and a chain's (or the whole ecosystem's) historical
TVL. All keyless.
}
\details{
The current-TVL \code{tvl} column and the historical series are measurements typed
\verb{| NA}: DefiLlama legitimately reports no TVL for some protocols (e.g. a pure DEX
that has volume but no locked value), and omits the field rather than reporting 0.

Inherits from \link{DefiLlamaBase}; all methods honour the \code{async} flag set at
construction (sync returns a \code{data.table}, async a promise).
}
\examples{
\dontrun{
tvl <- DefiLlamaProtocols$new()
# Every protocol with current TVL:
tvl$get_protocols()
# One protocol's full historical TVL:
tvl$get_protocol("aave")
# Current TVL per chain:
tvl$get_chains()
# Historical TVL of one chain (or omit for the whole DeFi aggregate):
tvl$get_historical_chain_tvl("Ethereum")
}

}
\section{Super classes}{
\code{\link[connectcore:RestClient]{connectcore::RestClient}} -> \code{\link[defillama:DefiLlamaBase]{defillama::DefiLlamaBase}} -> \code{DefiLlamaProtocols}
}
\section{Methods}{
\subsection{Public methods}{
\itemize{
\item \href{#method-DefiLlamaProtocols-new}{\code{DefiLlamaProtocols$new()}}
\item \href{#method-DefiLlamaProtocols-get_protocols}{\code{DefiLlamaProtocols$get_protocols()}}
\item \href{#method-DefiLlamaProtocols-get_protocol}{\code{DefiLlamaProtocols$get_protocol()}}
\item \href{#method-DefiLlamaProtocols-get_chains}{\code{DefiLlamaProtocols$get_chains()}}
\item \href{#method-DefiLlamaProtocols-get_historical_chain_tvl}{\code{DefiLlamaProtocols$get_historical_chain_tvl()}}
\item \href{#method-DefiLlamaProtocols-clone}{\code{DefiLlamaProtocols$clone()}}
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-DefiLlamaProtocols-new"></a>}}
\if{latex}{\out{\hypertarget{method-DefiLlamaProtocols-new}{}}}
\subsection{Method \code{new()}}{
Initialise a DefiLlamaProtocols client.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{DefiLlamaProtocols$new(
  base_url = defillama_base_url(),
  async = FALSE,
  max_tries = 3L
)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{base_url}}{(scalar<character>) the main API base URL. Defaults to
\code{\link[=defillama_base_url]{defillama_base_url()}}.}

\item{\code{async}}{(scalar<logical>) if \code{TRUE}, methods return promises. Default
\code{FALSE}.}

\item{\code{max_tries}}{(scalar<count in [1, Inf[>) retry budget for the idempotent
GET requests. Default \code{3}.}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
(class<DefiLlamaProtocols>) invisibly, self.
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-DefiLlamaProtocols-get_protocols"></a>}}
\if{latex}{\out{\hypertarget{method-DefiLlamaProtocols-get_protocols}{}}}
\subsection{Method \code{get_protocols()}}{
List every tracked DeFi protocol with its current TVL and identity.
One row per protocol.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{DefiLlamaProtocols$get_protocols()}\if{html}{\out{</div>}}
}

\subsection{Returns}{
(Protocols | promise<Protocols>) one row per protocol, or a promise
thereof.
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-DefiLlamaProtocols-get_protocol"></a>}}
\if{latex}{\out{\hypertarget{method-DefiLlamaProtocols-get_protocol}{}}}
\subsection{Method \code{get_protocol()}}{
The full historical TVL of one protocol, one row per UTC day.
Sourced from the rich \verb{/protocol/\{slug\}} object (only its top-level \code{tvl}
series is surfaced).
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{DefiLlamaProtocols$get_protocol(slug)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{slug}}{(scalar<character>) the protocol slug (from
\code{get_protocols()$slug}), e.g. \code{"aave"}.}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
(ProtocolTvl | promise<ProtocolTvl>) one row per UTC day, or a promise
thereof.
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-DefiLlamaProtocols-get_chains"></a>}}
\if{latex}{\out{\hypertarget{method-DefiLlamaProtocols-get_chains}{}}}
\subsection{Method \code{get_chains()}}{
The current aggregate DeFi TVL of every chain. One row per chain.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{DefiLlamaProtocols$get_chains()}\if{html}{\out{</div>}}
}

\subsection{Returns}{
(ChainTvl | promise<ChainTvl>) one row per chain, or a promise thereof.
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-DefiLlamaProtocols-get_historical_chain_tvl"></a>}}
\if{latex}{\out{\hypertarget{method-DefiLlamaProtocols-get_historical_chain_tvl}{}}}
\subsection{Method \code{get_historical_chain_tvl()}}{
The historical aggregate TVL of one chain, or of the whole DeFi
ecosystem when no chain is given. One row per UTC day.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{DefiLlamaProtocols$get_historical_chain_tvl(chain = NULL)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{chain}}{(scalar<character> | NULL) the chain, e.g. \code{"Ethereum"}; \code{NULL}
(default) returns the all-chains DeFi aggregate.}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
(ChainTvlHistory | promise<ChainTvlHistory>) one row per UTC day, or a
promise thereof.
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-DefiLlamaProtocols-clone"></a>}}
\if{latex}{\out{\hypertarget{method-DefiLlamaProtocols-clone}{}}}
\subsection{Method \code{clone()}}{
The objects of this class are cloneable with this method.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{DefiLlamaProtocols$clone(deep = FALSE)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{deep}}{Whether to make a deep clone.}
}
\if{html}{\out{</div>}}
}
}
}
