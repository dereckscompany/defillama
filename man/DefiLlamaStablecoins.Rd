% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DefiLlamaStablecoins.R
\name{DefiLlamaStablecoins}
\alias{DefiLlamaStablecoins}
\title{DefiLlamaStablecoins: Stablecoin Circulating Supply and Market Cap}
\description{
Retrieves DefiLlama's stablecoins data: the cross-sectional list of every tracked
stablecoin with its current circulating supply and price, the full daily
circulating-supply history of one stablecoin (or the aggregate of all), and the
current stablecoin market cap per chain. All keyless.
}
\details{
DefiLlama nests each supply measure under its \strong{peg type} key (e.g.
\code{{"peggedUSD": 1.84e11}}); this client unwraps the value faithfully and records
the peg type in a \code{peg_type} column. The optional history measures
(\code{total_unreleased}, \code{total_minted_usd}, \code{total_bridged_to_usd}) are present on
only a minority of days — an absent measure is \code{NA}, never \code{0}, so a genuine zero
and a missing value stay distinguishable.

Inherits from \link{DefiLlamaBase}; all methods honour the \code{async} flag set at
construction (sync returns a \code{data.table}, async a promise).
}
\examples{
\dontrun{
sc <- DefiLlamaStablecoins$new()
# Every tracked stablecoin, current supply and price:
sc$get_stablecoins()
# The full daily history of Tether (pegged-asset id 1):
sc$get_stablecoin_charts(stablecoin_id = 1)
# The aggregate history across all stablecoins:
sc$get_stablecoin_charts()
# Current stablecoin market cap by chain:
sc$get_stablecoin_chains()
}

}
\section{Super classes}{
\code{\link[connectcore:RestClient]{connectcore::RestClient}} -> \code{\link[defillama:DefiLlamaBase]{defillama::DefiLlamaBase}} -> \code{DefiLlamaStablecoins}
}
\section{Methods}{
\subsection{Public methods}{
\itemize{
\item \href{#method-DefiLlamaStablecoins-new}{\code{DefiLlamaStablecoins$new()}}
\item \href{#method-DefiLlamaStablecoins-get_stablecoins}{\code{DefiLlamaStablecoins$get_stablecoins()}}
\item \href{#method-DefiLlamaStablecoins-get_stablecoin_charts}{\code{DefiLlamaStablecoins$get_stablecoin_charts()}}
\item \href{#method-DefiLlamaStablecoins-get_stablecoin_chains}{\code{DefiLlamaStablecoins$get_stablecoin_chains()}}
\item \href{#method-DefiLlamaStablecoins-clone}{\code{DefiLlamaStablecoins$clone()}}
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-DefiLlamaStablecoins-new"></a>}}
\if{latex}{\out{\hypertarget{method-DefiLlamaStablecoins-new}{}}}
\subsection{Method \code{new()}}{
Initialise a DefiLlamaStablecoins client.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{DefiLlamaStablecoins$new(
  base_url = defillama_stablecoins_url(),
  async = FALSE,
  max_tries = 3L
)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{base_url}}{(scalar<character>) the stablecoins API base URL. Defaults to
\code{\link[=defillama_stablecoins_url]{defillama_stablecoins_url()}}.}

\item{\code{async}}{(scalar<logical>) if \code{TRUE}, methods return promises. Default
\code{FALSE}.}

\item{\code{max_tries}}{(scalar<count in [1, Inf[>) retry budget for the idempotent
GET requests. Default \code{3}.}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
(class<DefiLlamaStablecoins>) invisibly, self.
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-DefiLlamaStablecoins-get_stablecoins"></a>}}
\if{latex}{\out{\hypertarget{method-DefiLlamaStablecoins-get_stablecoins}{}}}
\subsection{Method \code{get_stablecoins()}}{
List every tracked stablecoin with its current circulating supply
and (optionally) price. One row per stablecoin.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{DefiLlamaStablecoins$get_stablecoins(include_prices = TRUE)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{include_prices}}{(scalar<logical>) request the latest price of each
stablecoin (the \code{includePrices} query flag). Default \code{TRUE}; \code{FALSE} leaves
the \code{price} column \code{NA}.}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
(PeggedAssets | promise<PeggedAssets>) one row per stablecoin, or a
promise thereof.
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-DefiLlamaStablecoins-get_stablecoin_charts"></a>}}
\if{latex}{\out{\hypertarget{method-DefiLlamaStablecoins-get_stablecoin_charts}{}}}
\subsection{Method \code{get_stablecoin_charts()}}{
The full daily circulating-supply and USD-market-cap history of
one stablecoin (by pegged-asset id), or the aggregate of all stablecoins when
no id is given. Optionally scoped to a single chain. The whole history comes
in one call (no pagination). One row per UTC day.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{DefiLlamaStablecoins$get_stablecoin_charts(stablecoin_id = NULL, chain = NULL)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{stablecoin_id}}{(scalar<character> | scalar<count in [1, Inf[> | NULL) the
DefiLlama pegged-asset id (from \code{get_stablecoins()$id}), e.g. \code{1} for Tether;
\code{NULL} (default) returns the all-stablecoins aggregate.}

\item{\code{chain}}{(scalar<character> | NULL) restrict to one chain, e.g.
\code{"Ethereum"}; \code{NULL} (default) aggregates across all chains.}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
(StablecoinCharts | promise<StablecoinCharts>) one row per UTC day, or a
promise thereof.
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-DefiLlamaStablecoins-get_stablecoin_chains"></a>}}
\if{latex}{\out{\hypertarget{method-DefiLlamaStablecoins-get_stablecoin_chains}{}}}
\subsection{Method \code{get_stablecoin_chains()}}{
The current total stablecoin market cap on each chain. One row per
chain.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{DefiLlamaStablecoins$get_stablecoin_chains()}\if{html}{\out{</div>}}
}

\subsection{Returns}{
(StablecoinChains | promise<StablecoinChains>) one row per chain, or a
promise thereof.
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-DefiLlamaStablecoins-clone"></a>}}
\if{latex}{\out{\hypertarget{method-DefiLlamaStablecoins-clone}{}}}
\subsection{Method \code{clone()}}{
The objects of this class are cloneable with this method.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{DefiLlamaStablecoins$clone(deep = FALSE)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{deep}}{Whether to make a deep clone.}
}
\if{html}{\out{</div>}}
}
}
}
