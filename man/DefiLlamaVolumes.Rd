% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DefiLlamaVolumes.R
\name{DefiLlamaVolumes}
\alias{DefiLlamaVolumes}
\title{DefiLlamaVolumes: Decentralized-Exchange Trading Volumes}
\description{
Retrieves DefiLlama's DEX-volumes overview: the per-DEX volume breakdown (one row
per protocol, with trailing 24h/7d/30d/1y/all-time totals) and the aggregate daily
volume time series, optionally scoped to a single chain. All keyless.
}
\details{
The \code{data_type} argument (one of \link{DEX_DATA_TYPES}) selects which volume series the
totals report; it is passed verbatim to the DefiLlama \code{dataType} query parameter.
The per-protocol \verb{total*} columns are measurements typed \verb{| NA} — a total is absent
until the protocol has that much history.

Inherits from \link{DefiLlamaBase}; all methods honour the \code{async} flag set at
construction (sync returns a \code{data.table}, async a promise).
}
\examples{
\dontrun{
vol <- DefiLlamaVolumes$new()
# Per-DEX volume breakdown across all chains:
vol$get_dexs_overview()
# Scoped to one chain:
vol$get_dexs_overview(chain = "ethereum")
# The aggregate daily DEX-volume history:
vol$get_dexs_chart()
}

}
\section{Super classes}{
\code{\link[connectcore:RestClient]{connectcore::RestClient}} -> \code{\link[defillama:DefiLlamaBase]{defillama::DefiLlamaBase}} -> \code{DefiLlamaVolumes}
}
\section{Methods}{
\subsection{Public methods}{
\itemize{
\item \href{#method-DefiLlamaVolumes-new}{\code{DefiLlamaVolumes$new()}}
\item \href{#method-DefiLlamaVolumes-get_dexs_overview}{\code{DefiLlamaVolumes$get_dexs_overview()}}
\item \href{#method-DefiLlamaVolumes-get_dexs_chart}{\code{DefiLlamaVolumes$get_dexs_chart()}}
\item \href{#method-DefiLlamaVolumes-clone}{\code{DefiLlamaVolumes$clone()}}
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-DefiLlamaVolumes-new"></a>}}
\if{latex}{\out{\hypertarget{method-DefiLlamaVolumes-new}{}}}
\subsection{Method \code{new()}}{
Initialise a DefiLlamaVolumes client.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{DefiLlamaVolumes$new(
  base_url = defillama_base_url(),
  async = FALSE,
  max_tries = 3L
)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{base_url}}{(scalar<character>) the main API base URL. Defaults to
\code{\link[=defillama_base_url]{defillama_base_url()}}.}

\item{\code{async}}{(scalar<logical>) if \code{TRUE}, methods return promises. Default
\code{FALSE}.}

\item{\code{max_tries}}{(scalar<count in [1, Inf[>) retry budget for the idempotent
GET requests. Default \code{3}.}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
(class<DefiLlamaVolumes>) invisibly, self.
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-DefiLlamaVolumes-get_dexs_overview"></a>}}
\if{latex}{\out{\hypertarget{method-DefiLlamaVolumes-get_dexs_overview}{}}}
\subsection{Method \code{get_dexs_overview()}}{
The per-DEX volume breakdown: one row per protocol with its
trailing 24h/7d/30d/1y/all-time volume totals. Optionally scoped to a single
chain.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{DefiLlamaVolumes$get_dexs_overview(
  chain = NULL,
  data_type = DEX_DATA_TYPES[["daily_volume"]]
)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{chain}}{(scalar<character> | NULL) restrict to one chain, e.g.
\code{"ethereum"}; \code{NULL} (default) covers all chains.}

\item{\code{data_type}}{(scalar<character>) the volume series to report, one of
\link{DEX_DATA_TYPES}. Default \code{"dailyVolume"}.}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
(DimensionOverview | promise<DimensionOverview>) one row per DEX, or a
promise thereof.
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-DefiLlamaVolumes-get_dexs_chart"></a>}}
\if{latex}{\out{\hypertarget{method-DefiLlamaVolumes-get_dexs_chart}{}}}
\subsection{Method \code{get_dexs_chart()}}{
The aggregate daily DEX-volume time series (the overview's
\code{totalDataChart}), one row per UTC day. Optionally scoped to a single chain.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{DefiLlamaVolumes$get_dexs_chart(
  chain = NULL,
  data_type = DEX_DATA_TYPES[["daily_volume"]]
)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{chain}}{(scalar<character> | NULL) restrict to one chain; \code{NULL} (default)
covers all chains.}

\item{\code{data_type}}{(scalar<character>) the volume series to report, one of
\link{DEX_DATA_TYPES}. Default \code{"dailyVolume"}.}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
(DimensionChart | promise<DimensionChart>) one row per UTC day, or a
promise thereof.
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-DefiLlamaVolumes-clone"></a>}}
\if{latex}{\out{\hypertarget{method-DefiLlamaVolumes-clone}{}}}
\subsection{Method \code{clone()}}{
The objects of this class are cloneable with this method.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{DefiLlamaVolumes$clone(deep = FALSE)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{deep}}{Whether to make a deep clone.}
}
\if{html}{\out{</div>}}
}
}
}
